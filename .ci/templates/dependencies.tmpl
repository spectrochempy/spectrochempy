{#-
---------------------------------------------------------------------------------------
DEPENDENCIES template
---------------------------------------------------------------------------------------
Change SpectroChemPy project dependencies here only.

requirements[_dev][_test].text, environment[_dev][_test].yml and recipes/meta.yaml will
be created or updated accordingly
#}
    # CORE dependencies
    # -----------------
    - brukeropusreader
    - colorama
    - dill
    - docrep
    - ipython
    - jinja2
    - numpy
    - matplotlib
    - osqp
    - pint
    - pyyaml
    - quaternion
    - requests
    - scikit-learn
    - setuptools_scm
    - tzlocal
    - xlrd
{%- if INTERACTIVE %}

    # INTERACTIVE dependencies
    # ------------------------
    - jupyterlab
    - ipykernel
    - ipyparallel
    - jupyterlab
    - nodejs
    - jupyter_server
{%- endif %}
{%- if TEST %}

    # TEST dependencies
    # ----------------
    - cffconvert
    - coverage
    - ipywidgets
    - pyfakefs
    - pytest
    - pytest-doctestplus
    - pytest-flake8
    - pytest-mock
    - numpydoc
{%- endif %}
{%- if DOCS %}

    # DOCS dependencies
    # -----------------
    - cffconvert
    - ipywidgets
    - jupytext
    - nbsphinx
    - sphinx
    - sphinx-copybutton
    - sphinx-gallery
    - sphinx_rtd_theme
    - sphinxcontrib-bibtex
    - json5
{%- endif %}
{%- if STYLING %}

    # STYLING dependencies
    # -------------------
    # From here, the dependencies are essentially for development.
    # They should not be necessary for the user of spectrochempy.
    - flake8
    - pep8-naming
    - black
    - isort
    - pre-commit
{%- endif %}
{%- if CANTERA %}

    # CANTERA dependencies
    # --------------------
    - cantera
{%- endif %}
