package:
  name: spectrochempy
  version: {{GIT_FULL_HASH}}

source:
  git_tag: {{GIT_FULL_HASH}}
  git_url: https://fernandezc@bitbucket.org/spectrocat/spectrochempy.git

requirements:
  build:
    - python

  run:
    - python        3.6.*
    - matplotlib    2.0.*
    - traitlets
    - numpy
    - scipy
    - pandas
    - xlrd
    - ipython
    - ipykernel
    - nb_conda
    - pytest
    - pytest-runner
    - scikit-image

#test:
  # Python imports
#  imports:
#    - spectrochempy
#    - spectrochempy.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    # Put any additional test requirements here.  For example
    - pytest
    - pytest-runner
    - scikit-image

about:
  home: http://www-lcs.ensicaen.fr/cfnews/spectrochempy/html
  license: CeCILL 2.1 License
  summary: 'SpectroChemPy is a framework for processing, analysing and modelling Spectroscopic data for Chemistry with Python'
  license_family: OTHER

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
