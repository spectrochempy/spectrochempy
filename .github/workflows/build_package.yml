# This workflow builds and publishes SpectroChemPy packages to:
# - PyPI
# - Anaconda Cloud
#
# Conda publication strategy:
#   - Non-release builds (pushes, PRs) are uploaded to the Anaconda dev label.
#   - Release builds are uploaded to the main (stable) label.
#   - The dev label is used for testing; stable packages should not be overwritten.
#   - Plugin conda recipes should declare a bounded dependency on the core
#     package, e.g.  spectrochempy >=0.8,<0.10

name: Build and publish packages 📦

on:
  push:
    branches:
      - master
      - fix/*

  pull_request:

  release:
    types: [published]

  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish_pypi:
    name: Build and publish distribution to PyPI
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install pypa/build
        run: |
          python -m pip install build --user

      - name: Build a binary wheel and a source tarball
        run: |
          python -m build --sdist --wheel --outdir dist/ .

      - name: Upload PyPI build artifacts
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v7
        with:
          name: pypi-package
          path: |
            dist/*.whl
            dist/*.tar.gz
          retention-days: 5

      - name: Publish package to Test PyPI
        if: github.event_name == 'push' && github.repository == 'spectrochempy/spectrochempy'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

      - name: Publish package to PyPI
        if: github.event_name == 'release' && github.event.action == 'published' && github.repository == 'spectrochempy/spectrochempy'
        uses: pypa/gh-action-pypi-publish@release/v1

  build_and_publish_conda_package:
    name: Build and publish conda package to Anaconda.org
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Conda environment with Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environments/environment_build.yml
          environment-name: scpy_build
          create-args: >-
            python=3.11
          cache-environment: true

      - name: Generate conda recipe
        run: |
          python3 .github/workflows/scripts/generate_conda_recipe.py
          cat recipe/recipe.yaml

      - name: Build conda package
        uses: prefix-dev/rattler-build-action@v0.2.38
        env:
          CONDA_BLD_PATH: ../output
        with:
          recipe-path: recipe/recipe.yaml
          upload-artifact: false
          build-args: "-c conda-forge -c spectrocat"

      - name: Move conda build output
        run: |
          mv ../output .

      - name: Upload conda build artifacts
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v7
        with:
          name: conda-package
          path: |
            output/**/*.conda
            output/**/*.tar.bz2
          retention-days: 5

      - name: Upload to Anaconda.org
        if: |
          github.repository == 'spectrochempy/spectrochempy' &&
          (github.event_name == 'release' || github.event_name == 'push')
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        run: |
          set -euo pipefail
          shopt -s nullglob globstar

          if [ -z "${ANACONDA_API_TOKEN:-}" ]; then
            echo "::error::ANACONDA_API_TOKEN is not set. Cannot upload to Anaconda.org."
            exit 1
          fi

          upload_package() {
            local label="$1"
            local force_flag="$2"
            local packages=(output/**/*.tar.bz2 output/**/*.conda)

            if [ ${#packages[@]} -eq 0 ]; then
              echo "::error::No conda packages found in output/"
              exit 1
            fi

            for package in "${packages[@]}"; do
              echo "Uploading $package with label: $label"
              anaconda --token "$ANACONDA_API_TOKEN" whoami
              anaconda --token "$ANACONDA_API_TOKEN" show spectrocat/spectrochempy
              anaconda --token "$ANACONDA_API_TOKEN" upload $force_flag -l "$label" "$package"
            done
          }

          if [ "${{ github.event_name }}" = "release" ]; then
            # Stable label: do not force overwrite. If a version already exists
            # the upload will fail cleanly, preventing accidental republishing.
            upload_package "main" ""
          else
            # Dev label: allow overwrite so the latest dev build is always available.
            upload_package "dev" "--force"
          fi

  discover-conda-plugins:
    name: Discover plugins with conda recipes
    runs-on: ubuntu-latest
    needs: build_and_publish_conda_package
    if: github.repository == 'spectrochempy/spectrochempy'
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set plugin matrix
        id: set-matrix
        run: |
          # Exclude plugin-template — it is a developer scaffold, not an official package.
          plugins=()
          for dir in plugins/*/; do
            name=$(basename "$dir")
            if [ "$name" = "plugin-template" ]; then
              echo "Skipping $name (developer template, not publishable)"
              continue
            fi
            if [ -f "${dir}recipe.yaml" ] || [ -f "${dir}meta.yaml" ]; then
              recipe_file="recipe.yaml"
              [ -f "${dir}meta.yaml" ] && recipe_file="meta.yaml"
              plugins+=("{\"name\":\"$name\",\"recipe\":\"${dir%/}\",\"recipe_file\":\"$recipe_file\"}")
            fi
          done

          if [ ${#plugins[@]} -eq 0 ]; then
            echo "No plugins with conda recipes found. Skipping plugin builds."
            echo "matrix={}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          joined=$(IFS=,; echo "${plugins[*]}")
          echo "matrix={\"plugin\":[$joined]}" >> "$GITHUB_OUTPUT"
          echo "Discovered conda plugins: $joined"

  build_and_publish_conda_plugins:
    name: Build and publish conda plugin packages to Anaconda.org
    runs-on: ubuntu-latest
    needs: discover-conda-plugins
    if: |
      github.repository == 'spectrochempy/spectrochempy' &&
      needs.discover-conda-plugins.outputs.matrix != '{}' &&
      needs.discover-conda-plugins.outputs.matrix != ''

    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.discover-conda-plugins.outputs.matrix) }}

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Conda environment with Micromamba
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environments/environment_build.yml
          environment-name: scpy_build
          create-args: >-
            python=3.11
          cache-environment: true

      - name: Update recipe version from release tag
        if: github.event_name == 'release'
        run: |
          # Extract version from tag such as spectrochempy-nmr-v0.1.1 → 0.1.1
          tag="${{ github.ref_name }}"
          version="${tag#*-v}"
          sed -i "s/version: \".*\"/version: \"$version\"/" ${{ matrix.plugin.recipe }}/${{ matrix.plugin.recipe_file }}
          echo "Updated recipe version to $version from tag $tag"
          cat ${{ matrix.plugin.recipe }}/${{ matrix.plugin.recipe_file }}

      - name: Build conda plugin package
        uses: prefix-dev/rattler-build-action@v0.2.38
        env:
          CONDA_BLD_PATH: ../output
        with:
          recipe-path: ${{ matrix.plugin.recipe }}/${{ matrix.plugin.recipe_file }}
          upload-artifact: false
          # For non-release builds, resolve dependencies from the dev label so that
          # dev plugin packages can depend on the latest dev core package.
          build-args: >-
            -c conda-forge
            -c spectrocat/label/dev
            -c spectrocat

      - name: Move conda build output
        run: |
          mv ../output .

      - name: Upload conda build artifacts
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v7
        with:
          name: conda-package-${{ matrix.plugin.name }}
          path: |
            output/**/*.conda
            output/**/*.tar.bz2
          retention-days: 5

      - name: Upload plugin to Anaconda.org
        if: |
          github.repository == 'spectrochempy/spectrochempy' &&
          (
            github.event_name == 'push' ||
            (github.event_name == 'release' && startsWith(github.ref_name, matrix.plugin.name))
          )
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        run: |
          set -euo pipefail
          shopt -s nullglob globstar

          if [ -z "${ANACONDA_API_TOKEN:-}" ]; then
            echo "::error::ANACONDA_API_TOKEN is not set. Cannot upload to Anaconda.org."
            exit 1
          fi

          packages=(output/**/*.tar.bz2 output/**/*.conda)
          if [ ${#packages[@]} -eq 0 ]; then
            echo "::error::No conda packages found in output/"
            exit 1
          fi

          label="dev"
          force_flag="--force"
          if [ "${{ github.event_name }}" = "release" ]; then
            label="main"
            # Do NOT force overwrite on the stable label. If a version already
            # exists the upload will fail cleanly, preventing accidental republishing.
            force_flag=""
          fi

          for package in "${packages[@]}"; do
            echo "Uploading $package with label: $label"
            anaconda --token "$ANACONDA_API_TOKEN" upload $force_flag -l "$label" "$package"
          done
